package com.green.day11.ch6;

public class ObjectStudy1 {
    public static void main(String[] args) {
        /*
        OOP (Objected-Orient Programming) - 객체지향
        UI (User-Interface), UX (User Experience)
        Framework, Library

        Java의 특징
        - OS에 독립적 > JVM (JRE안에 포함되어 있음)
        - 객체지향언어
        - 닷컴 붐이 불었을 때 수혜

        2. 객체지향 컨셉
        - class, prototype

        3. class, 객체, instance 관계

        4. 객체의 구성요소 (
            멤버 필드 == 속성 == property == 멤버 변수
            (멤버) 메소드 == 기능
            )
            예를들어,
            TV
            속성 : 크기, 높이, 길이, 색상, 볼륨, 채널, 만든회사 등등
            메소드 : 켜기, 끄기, 볼륨 올리기, 볼륨 낮추기, 채널 변경하기, 음소거 등등

        5. 객체 생성 (인스턴스화, 객체화) 문법
           클래스명 변수명 = new 클래스명();
           String str = "ddddd"; << String만 new 없이 쓸 수 있음. 이유 : String은 자주 써서..

           Tv tv = new Tv(); // Tv() << 기본생성자

         */
    }
}
